defmodule Advent2017.Day6 do
  @doc ~S"""
  Finds the most populated bank in `list`, and redistrubutes it across the
  other banks.

      iex> Advent2017.Day6.reallocate([0, 2, 7, 0])
      [2, 4, 1, 2]
  """
  @spec reallocate(list) :: list
  def reallocate(list) do
    list
  end
end
